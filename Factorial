#include <bits/stdc++.h>
using namespace std;

long long func(long long n) {
    if (n == 0) return 1;
    
    return n * func(n - 1);
}

int main() {
    long long n;
    cin >> n;
    cout << func(n) << endl;
}
