#include <bits/stdc++.h>
using namespace std;

int func(long long n, int count) {
    if(n == 1) return count;
    
    if(n % 2 == 1) return func((3 * n) + 1, count + 1);
    else return func(n / 2, count + 1);
}

int main() {
    long long n;
    cin >> n;
    
    cout << func(n, 1) << endl;
}
